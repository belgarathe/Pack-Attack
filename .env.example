# Pack-Attack Environment Configuration
# Copy this file to .env and fill in your values
# NEVER commit .env files with real credentials!

# ===========================================
# DATABASE (Neon PostgreSQL)
# ===========================================
# IMPORTANT: For Neon serverless databases, use TWO URLs:
# - DATABASE_URL: Pooled connection for application queries (use -pooler host)
# - DIRECT_DATABASE_URL: Direct connection for migrations (use non-pooler host)
#
# Pooled URL includes connection pool settings for stability:
# - connection_limit: Max connections in pool (recommended: 10)
# - pool_timeout: Seconds to wait for connection (recommended: 30)
DATABASE_URL="postgresql://username:password@host-pooler.region.aws.neon.tech/database?sslmode=require&connection_limit=10&pool_timeout=30"
# Direct URL for migrations (no -pooler in hostname)
DIRECT_DATABASE_URL="postgresql://username:password@host.region.aws.neon.tech/database?sslmode=require"

# ===========================================
# AUTHENTICATION (NextAuth.js)
# ===========================================
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="generate-with-openssl-rand-base64-32"

# ===========================================
# ADMIN USER (for create-admin script)
# ===========================================
ADMIN_EMAIL="admin@yourdomain.com"
ADMIN_PASSWORD="your-secure-password-min-8-chars"
ADMIN_NAME="Admin User"

# ===========================================
# SERVER CONFIGURATION
# ===========================================
SERVER_IP="your-server-ip"
NODE_ENV="production"

# ===========================================
# EMAIL (Resend)
# ===========================================
RESEND_API_KEY="re_your_api_key_here"
RESEND_FROM_EMAIL="onboarding@resend.dev"

# ===========================================
# CARD APIs
# ===========================================
# JustTCG API for One Piece cards (get key from justtcg.com)
JUSTTCG_API_KEY="tcg_your_api_key_here"
